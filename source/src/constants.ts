export const PORT = +(process.env.PORT || 8080);
export const TOPIC = process.env.TOPIC || 'docker';
export const VERSION = +(process.env.VERSION || 1);
export const ALIAS = process.env.ALIAS || '<none>';
